import { NextResponse } from 'next/server'; import { getCurrentUser } from '@/lib/auth'; import { createRunNow,getAutomation } from '@/lib/content-automation-repository'; import { createOperatorJobFromRequest } from '@/lib/operator-job-service';
export async function POST(request,{params}){try{const user=getCurrentUser(request);if(!user)return NextResponse.json({success:false,error:'Unauthorized'},{status:401});const {id}=await params,schedule=await getAutomation(id);if(!schedule)return NextResponse.json({success:false,error:'Not found'},{status:404});const run=await createRunNow(schedule);const payload=typeof schedule.operator_request_json==='string'?JSON.parse(schedule.operator_request_json):schedule.operator_request_json;const job=await createOperatorJobFromRequest({request:payload,idempotencyKey:run.idempotency_key,actor:`automation-run-now:${user.id}`});const {updateRun}=await import('@/lib/content-automation-repository');await updateRun(run.id,{operator_job_id:job.id,status:'job_created'});return NextResponse.json({success:true,run_id:run.id,operator_job_id:job.id});}catch(e){return NextResponse.json({success:false,code:e.code,error:e.message},{status:e.status||500});}}
