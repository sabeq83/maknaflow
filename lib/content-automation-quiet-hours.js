function localMinutes(date,timezone){const parts=new Intl.DateTimeFormat('en-GB',{timeZone:timezone,hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(date);const values=Object.fromEntries(parts.map(x=>[x.type,x.value]));return Number(values.hour)*60+Number(values.minute);}
function parseTime(value){if(!/^\d{2}:\d{2}$/.test(value||''))return null;const [h,m]=value.split(':').map(Number);return h<24&&m<60?h*60+m:null;}
export function isInQuietHours({date=new Date(),start,end,timezone='Asia/Jakarta'}){const a=parseTime(start),b=parseTime(end);if(a===null||b===null||a===b)return false;const current=localMinutes(date,timezone);return a<b?current>=a&&current<b:current>=a||current<b;}
export function nextQuietHoursEnd(input){let cursor=new Date((input.date||new Date()).getTime()+60000);cursor.setUTCSeconds(0,0);for(let i=0;i<24*60+2;i++,cursor=new Date(cursor.getTime()+60000)){if(!isInQuietHours({...input,date:cursor}))return cursor;}return new Date((input.date||new Date()).getTime()+24*60*60000);}
